## Architecture review round

Before committing the delta, request one review from the `persona-architect`
agent (Mode B — a draft `architecture.md` exists). Fold its verdicts into
the draft, or record in the draft why a blocker was overridden. The
architecture becomes binding only after this round.
