## Phase: Complete (docs)

Prerequisites: `## Published` recorded in `spec.md`, working tree clean.

1. Run the documentation gate stages once more (or the recorded manual
   verification) — failures stop completion.
2. Spec frontmatter: `status: implemented`, `completed: [today]`.
3. Append ONE line to `specs/REGISTRY.md`, ≤50 words:
   `[folder] · [date] · docs/[rigor] · [tags] · [summary] · [PR/branch]`.

Then hand off per the review mode below.
